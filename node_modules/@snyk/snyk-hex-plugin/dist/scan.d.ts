import { PluginResponse } from './types';
interface Options {
    debug?: boolean;
    dev?: boolean;
    projectName?: string;
    path: string;
    targetFile?: string;
}
export declare function scan(options: Options): Promise<PluginResponse>;
export {};
