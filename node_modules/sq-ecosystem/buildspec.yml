version: 0.2

phases:
  pre_build:
    commands:
      - echo Docker login
      - docker login --username vdeturckheimsqci --password ${NODE_DOCKER_TOKEN}
  build:
    commands:
      - docker build . --build-arg node_version=${NODE_VERSION} --build-arg base_image=${BASE_IMAGE} -t agent/test
      - docker run agent/test npm run test-ci
