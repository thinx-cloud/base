import { EntityToFix, FixChangesSummary, FixOptions, RemediationChanges, Workspace } from '../../../../types';
import { PluginFixResponse } from '../../../types';
import { ParsedRequirements } from './update-dependencies/requirements-file-parser';
export declare function pipRequirementsTxt(entities: EntityToFix[], options: FixOptions): Promise<PluginFixResponse>;
export declare function getRequiredData(entity: EntityToFix): {
    remediation: RemediationChanges;
    targetFile: string;
    workspace: Workspace;
};
export declare function fixIndividualRequirementsTxt(workspace: Workspace, dir: string, entryFileName: string, fileName: string, remediation: RemediationChanges, parsedRequirements: ParsedRequirements, options: FixOptions, directUpgradesOnly: boolean): Promise<{
    changes: FixChangesSummary[];
    appliedRemediation: string[];
}>;
export declare function applyAllFixes(entity: EntityToFix, options: FixOptions): Promise<{
    changes: FixChangesSummary[];
    fixedFiles: string[];
}>;
