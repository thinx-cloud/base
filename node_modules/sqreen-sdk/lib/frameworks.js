'use strict';
const HTTPTrace = require('./traces/httpTrace');
module.exports.express = function () {

    return function (req, res, next) {

        req.SqreenTrace = new HTTPTrace(req, req.ip); // TODO: proper IP address detection
        next();
    };
};
