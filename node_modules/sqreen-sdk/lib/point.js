'use strict';
const Signal = require('./signal');
const TYPES = require('./signalTypes').TYPES;

module.exports = class Point extends Signal {

    /**
     *
     * @param {string} signalName
     * @param {string} source
     * @param {Date?} time
     */
    constructor(signalName, source , time) {

        super(TYPES.POINT, signalName, source, time);
    }
};
